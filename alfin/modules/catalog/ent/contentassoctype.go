// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/samlet/petrel/alfin/modules/catalog/ent/contentassoctype"
)

// ContentAssocType is the model entity for the ContentAssocType schema.
type ContentAssocType struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// StringRef holds the value of the "string_ref" field.
	StringRef string `json:"string_ref,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ContentAssocType) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case contentassoctype.FieldID:
			values[i] = new(sql.NullInt64)
		case contentassoctype.FieldStringRef, contentassoctype.FieldDescription:
			values[i] = new(sql.NullString)
		case contentassoctype.FieldCreateTime, contentassoctype.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type ContentAssocType", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ContentAssocType fields.
func (cat *ContentAssocType) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case contentassoctype.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			cat.ID = int(value.Int64)
		case contentassoctype.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				cat.CreateTime = value.Time
			}
		case contentassoctype.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				cat.UpdateTime = value.Time
			}
		case contentassoctype.FieldStringRef:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field string_ref", values[i])
			} else if value.Valid {
				cat.StringRef = value.String
			}
		case contentassoctype.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				cat.Description = value.String
			}
		}
	}
	return nil
}

// Update returns a builder for updating this ContentAssocType.
// Note that you need to call ContentAssocType.Unwrap() before calling this method if this ContentAssocType
// was returned from a transaction, and the transaction was committed or rolled back.
func (cat *ContentAssocType) Update() *ContentAssocTypeUpdateOne {
	return (&ContentAssocTypeClient{config: cat.config}).UpdateOne(cat)
}

// Unwrap unwraps the ContentAssocType entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (cat *ContentAssocType) Unwrap() *ContentAssocType {
	tx, ok := cat.config.driver.(*txDriver)
	if !ok {
		panic("ent: ContentAssocType is not a transactional entity")
	}
	cat.config.driver = tx.drv
	return cat
}

// String implements the fmt.Stringer.
func (cat *ContentAssocType) String() string {
	var builder strings.Builder
	builder.WriteString("ContentAssocType(")
	builder.WriteString(fmt.Sprintf("id=%v", cat.ID))
	builder.WriteString(", create_time=")
	builder.WriteString(cat.CreateTime.Format(time.ANSIC))
	builder.WriteString(", update_time=")
	builder.WriteString(cat.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", string_ref=")
	builder.WriteString(cat.StringRef)
	builder.WriteString(", description=")
	builder.WriteString(cat.Description)
	builder.WriteByte(')')
	return builder.String()
}

// ContentAssocTypes is a parsable slice of ContentAssocType.
type ContentAssocTypes []*ContentAssocType

func (cat ContentAssocTypes) config(cfg config) {
	for _i := range cat {
		cat[_i].config = cfg
	}
}
