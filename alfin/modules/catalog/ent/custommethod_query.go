// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/samlet/petrel/alfin/modules/catalog/ent/custommethod"
	"github.com/samlet/petrel/alfin/modules/catalog/ent/custommethodtype"
	"github.com/samlet/petrel/alfin/modules/catalog/ent/predicate"
	"github.com/samlet/petrel/alfin/modules/catalog/ent/productassoc"
	"github.com/samlet/petrel/alfin/modules/catalog/ent/productprice"
)

// CustomMethodQuery is the builder for querying CustomMethod entities.
type CustomMethodQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.CustomMethod
	// eager-loading edges.
	withCustomMethodType *CustomMethodTypeQuery
	withProductAssocs    *ProductAssocQuery
	withProductPrices    *ProductPriceQuery
	withFKs              bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CustomMethodQuery builder.
func (cmq *CustomMethodQuery) Where(ps ...predicate.CustomMethod) *CustomMethodQuery {
	cmq.predicates = append(cmq.predicates, ps...)
	return cmq
}

// Limit adds a limit step to the query.
func (cmq *CustomMethodQuery) Limit(limit int) *CustomMethodQuery {
	cmq.limit = &limit
	return cmq
}

// Offset adds an offset step to the query.
func (cmq *CustomMethodQuery) Offset(offset int) *CustomMethodQuery {
	cmq.offset = &offset
	return cmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cmq *CustomMethodQuery) Unique(unique bool) *CustomMethodQuery {
	cmq.unique = &unique
	return cmq
}

// Order adds an order step to the query.
func (cmq *CustomMethodQuery) Order(o ...OrderFunc) *CustomMethodQuery {
	cmq.order = append(cmq.order, o...)
	return cmq
}

// QueryCustomMethodType chains the current query on the "custom_method_type" edge.
func (cmq *CustomMethodQuery) QueryCustomMethodType() *CustomMethodTypeQuery {
	query := &CustomMethodTypeQuery{config: cmq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cmq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(custommethod.Table, custommethod.FieldID, selector),
			sqlgraph.To(custommethodtype.Table, custommethodtype.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, custommethod.CustomMethodTypeTable, custommethod.CustomMethodTypeColumn),
		)
		fromU = sqlgraph.SetNeighbors(cmq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryProductAssocs chains the current query on the "product_assocs" edge.
func (cmq *CustomMethodQuery) QueryProductAssocs() *ProductAssocQuery {
	query := &ProductAssocQuery{config: cmq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cmq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(custommethod.Table, custommethod.FieldID, selector),
			sqlgraph.To(productassoc.Table, productassoc.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, custommethod.ProductAssocsTable, custommethod.ProductAssocsColumn),
		)
		fromU = sqlgraph.SetNeighbors(cmq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryProductPrices chains the current query on the "product_prices" edge.
func (cmq *CustomMethodQuery) QueryProductPrices() *ProductPriceQuery {
	query := &ProductPriceQuery{config: cmq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cmq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(custommethod.Table, custommethod.FieldID, selector),
			sqlgraph.To(productprice.Table, productprice.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, custommethod.ProductPricesTable, custommethod.ProductPricesColumn),
		)
		fromU = sqlgraph.SetNeighbors(cmq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CustomMethod entity from the query.
// Returns a *NotFoundError when no CustomMethod was found.
func (cmq *CustomMethodQuery) First(ctx context.Context) (*CustomMethod, error) {
	nodes, err := cmq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{custommethod.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cmq *CustomMethodQuery) FirstX(ctx context.Context) *CustomMethod {
	node, err := cmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CustomMethod ID from the query.
// Returns a *NotFoundError when no CustomMethod ID was found.
func (cmq *CustomMethodQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cmq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{custommethod.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cmq *CustomMethodQuery) FirstIDX(ctx context.Context) int {
	id, err := cmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CustomMethod entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when exactly one CustomMethod entity is not found.
// Returns a *NotFoundError when no CustomMethod entities are found.
func (cmq *CustomMethodQuery) Only(ctx context.Context) (*CustomMethod, error) {
	nodes, err := cmq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{custommethod.Label}
	default:
		return nil, &NotSingularError{custommethod.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cmq *CustomMethodQuery) OnlyX(ctx context.Context) *CustomMethod {
	node, err := cmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CustomMethod ID in the query.
// Returns a *NotSingularError when exactly one CustomMethod ID is not found.
// Returns a *NotFoundError when no entities are found.
func (cmq *CustomMethodQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cmq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{custommethod.Label}
	default:
		err = &NotSingularError{custommethod.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cmq *CustomMethodQuery) OnlyIDX(ctx context.Context) int {
	id, err := cmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CustomMethods.
func (cmq *CustomMethodQuery) All(ctx context.Context) ([]*CustomMethod, error) {
	if err := cmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return cmq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (cmq *CustomMethodQuery) AllX(ctx context.Context) []*CustomMethod {
	nodes, err := cmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CustomMethod IDs.
func (cmq *CustomMethodQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := cmq.Select(custommethod.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cmq *CustomMethodQuery) IDsX(ctx context.Context) []int {
	ids, err := cmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cmq *CustomMethodQuery) Count(ctx context.Context) (int, error) {
	if err := cmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return cmq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (cmq *CustomMethodQuery) CountX(ctx context.Context) int {
	count, err := cmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cmq *CustomMethodQuery) Exist(ctx context.Context) (bool, error) {
	if err := cmq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return cmq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (cmq *CustomMethodQuery) ExistX(ctx context.Context) bool {
	exist, err := cmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CustomMethodQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cmq *CustomMethodQuery) Clone() *CustomMethodQuery {
	if cmq == nil {
		return nil
	}
	return &CustomMethodQuery{
		config:               cmq.config,
		limit:                cmq.limit,
		offset:               cmq.offset,
		order:                append([]OrderFunc{}, cmq.order...),
		predicates:           append([]predicate.CustomMethod{}, cmq.predicates...),
		withCustomMethodType: cmq.withCustomMethodType.Clone(),
		withProductAssocs:    cmq.withProductAssocs.Clone(),
		withProductPrices:    cmq.withProductPrices.Clone(),
		// clone intermediate query.
		sql:  cmq.sql.Clone(),
		path: cmq.path,
	}
}

// WithCustomMethodType tells the query-builder to eager-load the nodes that are connected to
// the "custom_method_type" edge. The optional arguments are used to configure the query builder of the edge.
func (cmq *CustomMethodQuery) WithCustomMethodType(opts ...func(*CustomMethodTypeQuery)) *CustomMethodQuery {
	query := &CustomMethodTypeQuery{config: cmq.config}
	for _, opt := range opts {
		opt(query)
	}
	cmq.withCustomMethodType = query
	return cmq
}

// WithProductAssocs tells the query-builder to eager-load the nodes that are connected to
// the "product_assocs" edge. The optional arguments are used to configure the query builder of the edge.
func (cmq *CustomMethodQuery) WithProductAssocs(opts ...func(*ProductAssocQuery)) *CustomMethodQuery {
	query := &ProductAssocQuery{config: cmq.config}
	for _, opt := range opts {
		opt(query)
	}
	cmq.withProductAssocs = query
	return cmq
}

// WithProductPrices tells the query-builder to eager-load the nodes that are connected to
// the "product_prices" edge. The optional arguments are used to configure the query builder of the edge.
func (cmq *CustomMethodQuery) WithProductPrices(opts ...func(*ProductPriceQuery)) *CustomMethodQuery {
	query := &ProductPriceQuery{config: cmq.config}
	for _, opt := range opts {
		opt(query)
	}
	cmq.withProductPrices = query
	return cmq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CustomMethod.Query().
//		GroupBy(custommethod.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (cmq *CustomMethodQuery) GroupBy(field string, fields ...string) *CustomMethodGroupBy {
	group := &CustomMethodGroupBy{config: cmq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := cmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return cmq.sqlQuery(ctx), nil
	}
	return group
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.CustomMethod.Query().
//		Select(custommethod.FieldCreateTime).
//		Scan(ctx, &v)
//
func (cmq *CustomMethodQuery) Select(field string, fields ...string) *CustomMethodSelect {
	cmq.fields = append([]string{field}, fields...)
	return &CustomMethodSelect{CustomMethodQuery: cmq}
}

func (cmq *CustomMethodQuery) prepareQuery(ctx context.Context) error {
	for _, f := range cmq.fields {
		if !custommethod.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cmq.path != nil {
		prev, err := cmq.path(ctx)
		if err != nil {
			return err
		}
		cmq.sql = prev
	}
	return nil
}

func (cmq *CustomMethodQuery) sqlAll(ctx context.Context) ([]*CustomMethod, error) {
	var (
		nodes       = []*CustomMethod{}
		withFKs     = cmq.withFKs
		_spec       = cmq.querySpec()
		loadedTypes = [3]bool{
			cmq.withCustomMethodType != nil,
			cmq.withProductAssocs != nil,
			cmq.withProductPrices != nil,
		}
	)
	if cmq.withCustomMethodType != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, custommethod.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		node := &CustomMethod{config: cmq.config}
		nodes = append(nodes, node)
		return node.scanValues(columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("ent: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if err := sqlgraph.QueryNodes(ctx, cmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := cmq.withCustomMethodType; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*CustomMethod)
		for i := range nodes {
			if nodes[i].custom_method_type_custom_methods == nil {
				continue
			}
			fk := *nodes[i].custom_method_type_custom_methods
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(custommethodtype.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "custom_method_type_custom_methods" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.CustomMethodType = n
			}
		}
	}

	if query := cmq.withProductAssocs; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int]*CustomMethod)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.ProductAssocs = []*ProductAssoc{}
		}
		query.withFKs = true
		query.Where(predicate.ProductAssoc(func(s *sql.Selector) {
			s.Where(sql.InValues(custommethod.ProductAssocsColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.custom_method_product_assocs
			if fk == nil {
				return nil, fmt.Errorf(`foreign-key "custom_method_product_assocs" is nil for node %v`, n.ID)
			}
			node, ok := nodeids[*fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "custom_method_product_assocs" returned %v for node %v`, *fk, n.ID)
			}
			node.Edges.ProductAssocs = append(node.Edges.ProductAssocs, n)
		}
	}

	if query := cmq.withProductPrices; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int]*CustomMethod)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.ProductPrices = []*ProductPrice{}
		}
		query.withFKs = true
		query.Where(predicate.ProductPrice(func(s *sql.Selector) {
			s.Where(sql.InValues(custommethod.ProductPricesColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.custom_method_product_prices
			if fk == nil {
				return nil, fmt.Errorf(`foreign-key "custom_method_product_prices" is nil for node %v`, n.ID)
			}
			node, ok := nodeids[*fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "custom_method_product_prices" returned %v for node %v`, *fk, n.ID)
			}
			node.Edges.ProductPrices = append(node.Edges.ProductPrices, n)
		}
	}

	return nodes, nil
}

func (cmq *CustomMethodQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cmq.querySpec()
	return sqlgraph.CountNodes(ctx, cmq.driver, _spec)
}

func (cmq *CustomMethodQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := cmq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (cmq *CustomMethodQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   custommethod.Table,
			Columns: custommethod.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: custommethod.FieldID,
			},
		},
		From:   cmq.sql,
		Unique: true,
	}
	if unique := cmq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := cmq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, custommethod.FieldID)
		for i := range fields {
			if fields[i] != custommethod.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cmq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cmq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cmq *CustomMethodQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cmq.driver.Dialect())
	t1 := builder.Table(custommethod.Table)
	columns := cmq.fields
	if len(columns) == 0 {
		columns = custommethod.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cmq.sql != nil {
		selector = cmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	for _, p := range cmq.predicates {
		p(selector)
	}
	for _, p := range cmq.order {
		p(selector)
	}
	if offset := cmq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cmq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CustomMethodGroupBy is the group-by builder for CustomMethod entities.
type CustomMethodGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cmgb *CustomMethodGroupBy) Aggregate(fns ...AggregateFunc) *CustomMethodGroupBy {
	cmgb.fns = append(cmgb.fns, fns...)
	return cmgb
}

// Scan applies the group-by query and scans the result into the given value.
func (cmgb *CustomMethodGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := cmgb.path(ctx)
	if err != nil {
		return err
	}
	cmgb.sql = query
	return cmgb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (cmgb *CustomMethodGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := cmgb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by.
// It is only allowed when executing a group-by query with one field.
func (cmgb *CustomMethodGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(cmgb.fields) > 1 {
		return nil, errors.New("ent: CustomMethodGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := cmgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (cmgb *CustomMethodGroupBy) StringsX(ctx context.Context) []string {
	v, err := cmgb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (cmgb *CustomMethodGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = cmgb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{custommethod.Label}
	default:
		err = fmt.Errorf("ent: CustomMethodGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (cmgb *CustomMethodGroupBy) StringX(ctx context.Context) string {
	v, err := cmgb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by.
// It is only allowed when executing a group-by query with one field.
func (cmgb *CustomMethodGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(cmgb.fields) > 1 {
		return nil, errors.New("ent: CustomMethodGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := cmgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (cmgb *CustomMethodGroupBy) IntsX(ctx context.Context) []int {
	v, err := cmgb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (cmgb *CustomMethodGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = cmgb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{custommethod.Label}
	default:
		err = fmt.Errorf("ent: CustomMethodGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (cmgb *CustomMethodGroupBy) IntX(ctx context.Context) int {
	v, err := cmgb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (cmgb *CustomMethodGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(cmgb.fields) > 1 {
		return nil, errors.New("ent: CustomMethodGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := cmgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (cmgb *CustomMethodGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := cmgb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (cmgb *CustomMethodGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = cmgb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{custommethod.Label}
	default:
		err = fmt.Errorf("ent: CustomMethodGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (cmgb *CustomMethodGroupBy) Float64X(ctx context.Context) float64 {
	v, err := cmgb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by.
// It is only allowed when executing a group-by query with one field.
func (cmgb *CustomMethodGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(cmgb.fields) > 1 {
		return nil, errors.New("ent: CustomMethodGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := cmgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (cmgb *CustomMethodGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := cmgb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (cmgb *CustomMethodGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = cmgb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{custommethod.Label}
	default:
		err = fmt.Errorf("ent: CustomMethodGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (cmgb *CustomMethodGroupBy) BoolX(ctx context.Context) bool {
	v, err := cmgb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (cmgb *CustomMethodGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range cmgb.fields {
		if !custommethod.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := cmgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cmgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (cmgb *CustomMethodGroupBy) sqlQuery() *sql.Selector {
	selector := cmgb.sql.Select()
	aggregation := make([]string, 0, len(cmgb.fns))
	for _, fn := range cmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(cmgb.fields)+len(cmgb.fns))
		for _, f := range cmgb.fields {
			columns = append(columns, selector.C(f))
		}
		for _, c := range aggregation {
			columns = append(columns, c)
		}
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(cmgb.fields...)...)
}

// CustomMethodSelect is the builder for selecting fields of CustomMethod entities.
type CustomMethodSelect struct {
	*CustomMethodQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (cms *CustomMethodSelect) Scan(ctx context.Context, v interface{}) error {
	if err := cms.prepareQuery(ctx); err != nil {
		return err
	}
	cms.sql = cms.CustomMethodQuery.sqlQuery(ctx)
	return cms.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (cms *CustomMethodSelect) ScanX(ctx context.Context, v interface{}) {
	if err := cms.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from a selector. It is only allowed when selecting one field.
func (cms *CustomMethodSelect) Strings(ctx context.Context) ([]string, error) {
	if len(cms.fields) > 1 {
		return nil, errors.New("ent: CustomMethodSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := cms.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (cms *CustomMethodSelect) StringsX(ctx context.Context) []string {
	v, err := cms.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a selector. It is only allowed when selecting one field.
func (cms *CustomMethodSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = cms.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{custommethod.Label}
	default:
		err = fmt.Errorf("ent: CustomMethodSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (cms *CustomMethodSelect) StringX(ctx context.Context) string {
	v, err := cms.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from a selector. It is only allowed when selecting one field.
func (cms *CustomMethodSelect) Ints(ctx context.Context) ([]int, error) {
	if len(cms.fields) > 1 {
		return nil, errors.New("ent: CustomMethodSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := cms.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (cms *CustomMethodSelect) IntsX(ctx context.Context) []int {
	v, err := cms.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a selector. It is only allowed when selecting one field.
func (cms *CustomMethodSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = cms.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{custommethod.Label}
	default:
		err = fmt.Errorf("ent: CustomMethodSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (cms *CustomMethodSelect) IntX(ctx context.Context) int {
	v, err := cms.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from a selector. It is only allowed when selecting one field.
func (cms *CustomMethodSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(cms.fields) > 1 {
		return nil, errors.New("ent: CustomMethodSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := cms.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (cms *CustomMethodSelect) Float64sX(ctx context.Context) []float64 {
	v, err := cms.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a selector. It is only allowed when selecting one field.
func (cms *CustomMethodSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = cms.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{custommethod.Label}
	default:
		err = fmt.Errorf("ent: CustomMethodSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (cms *CustomMethodSelect) Float64X(ctx context.Context) float64 {
	v, err := cms.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from a selector. It is only allowed when selecting one field.
func (cms *CustomMethodSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(cms.fields) > 1 {
		return nil, errors.New("ent: CustomMethodSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := cms.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (cms *CustomMethodSelect) BoolsX(ctx context.Context) []bool {
	v, err := cms.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a selector. It is only allowed when selecting one field.
func (cms *CustomMethodSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = cms.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{custommethod.Label}
	default:
		err = fmt.Errorf("ent: CustomMethodSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (cms *CustomMethodSelect) BoolX(ctx context.Context) bool {
	v, err := cms.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (cms *CustomMethodSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := cms.sql.Query()
	if err := cms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
