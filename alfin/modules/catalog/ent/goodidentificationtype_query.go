// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/samlet/petrel/alfin/modules/catalog/ent/goodidentificationtype"
	"github.com/samlet/petrel/alfin/modules/catalog/ent/predicate"
)

// GoodIdentificationTypeQuery is the builder for querying GoodIdentificationType entities.
type GoodIdentificationTypeQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.GoodIdentificationType
	// eager-loading edges.
	withParent                       *GoodIdentificationTypeQuery
	withChildren                     *GoodIdentificationTypeQuery
	withChildGoodIdentificationTypes *GoodIdentificationTypeQuery
	withFKs                          bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GoodIdentificationTypeQuery builder.
func (gitq *GoodIdentificationTypeQuery) Where(ps ...predicate.GoodIdentificationType) *GoodIdentificationTypeQuery {
	gitq.predicates = append(gitq.predicates, ps...)
	return gitq
}

// Limit adds a limit step to the query.
func (gitq *GoodIdentificationTypeQuery) Limit(limit int) *GoodIdentificationTypeQuery {
	gitq.limit = &limit
	return gitq
}

// Offset adds an offset step to the query.
func (gitq *GoodIdentificationTypeQuery) Offset(offset int) *GoodIdentificationTypeQuery {
	gitq.offset = &offset
	return gitq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (gitq *GoodIdentificationTypeQuery) Unique(unique bool) *GoodIdentificationTypeQuery {
	gitq.unique = &unique
	return gitq
}

// Order adds an order step to the query.
func (gitq *GoodIdentificationTypeQuery) Order(o ...OrderFunc) *GoodIdentificationTypeQuery {
	gitq.order = append(gitq.order, o...)
	return gitq
}

// QueryParent chains the current query on the "parent" edge.
func (gitq *GoodIdentificationTypeQuery) QueryParent() *GoodIdentificationTypeQuery {
	query := &GoodIdentificationTypeQuery{config: gitq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := gitq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := gitq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(goodidentificationtype.Table, goodidentificationtype.FieldID, selector),
			sqlgraph.To(goodidentificationtype.Table, goodidentificationtype.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, goodidentificationtype.ParentTable, goodidentificationtype.ParentColumn),
		)
		fromU = sqlgraph.SetNeighbors(gitq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChildren chains the current query on the "children" edge.
func (gitq *GoodIdentificationTypeQuery) QueryChildren() *GoodIdentificationTypeQuery {
	query := &GoodIdentificationTypeQuery{config: gitq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := gitq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := gitq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(goodidentificationtype.Table, goodidentificationtype.FieldID, selector),
			sqlgraph.To(goodidentificationtype.Table, goodidentificationtype.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, goodidentificationtype.ChildrenTable, goodidentificationtype.ChildrenColumn),
		)
		fromU = sqlgraph.SetNeighbors(gitq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChildGoodIdentificationTypes chains the current query on the "child_good_identification_types" edge.
func (gitq *GoodIdentificationTypeQuery) QueryChildGoodIdentificationTypes() *GoodIdentificationTypeQuery {
	query := &GoodIdentificationTypeQuery{config: gitq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := gitq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := gitq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(goodidentificationtype.Table, goodidentificationtype.FieldID, selector),
			sqlgraph.To(goodidentificationtype.Table, goodidentificationtype.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, goodidentificationtype.ChildGoodIdentificationTypesTable, goodidentificationtype.ChildGoodIdentificationTypesPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(gitq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first GoodIdentificationType entity from the query.
// Returns a *NotFoundError when no GoodIdentificationType was found.
func (gitq *GoodIdentificationTypeQuery) First(ctx context.Context) (*GoodIdentificationType, error) {
	nodes, err := gitq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{goodidentificationtype.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (gitq *GoodIdentificationTypeQuery) FirstX(ctx context.Context) *GoodIdentificationType {
	node, err := gitq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GoodIdentificationType ID from the query.
// Returns a *NotFoundError when no GoodIdentificationType ID was found.
func (gitq *GoodIdentificationTypeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = gitq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{goodidentificationtype.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (gitq *GoodIdentificationTypeQuery) FirstIDX(ctx context.Context) int {
	id, err := gitq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GoodIdentificationType entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when exactly one GoodIdentificationType entity is not found.
// Returns a *NotFoundError when no GoodIdentificationType entities are found.
func (gitq *GoodIdentificationTypeQuery) Only(ctx context.Context) (*GoodIdentificationType, error) {
	nodes, err := gitq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{goodidentificationtype.Label}
	default:
		return nil, &NotSingularError{goodidentificationtype.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (gitq *GoodIdentificationTypeQuery) OnlyX(ctx context.Context) *GoodIdentificationType {
	node, err := gitq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GoodIdentificationType ID in the query.
// Returns a *NotSingularError when exactly one GoodIdentificationType ID is not found.
// Returns a *NotFoundError when no entities are found.
func (gitq *GoodIdentificationTypeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = gitq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{goodidentificationtype.Label}
	default:
		err = &NotSingularError{goodidentificationtype.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (gitq *GoodIdentificationTypeQuery) OnlyIDX(ctx context.Context) int {
	id, err := gitq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GoodIdentificationTypes.
func (gitq *GoodIdentificationTypeQuery) All(ctx context.Context) ([]*GoodIdentificationType, error) {
	if err := gitq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return gitq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (gitq *GoodIdentificationTypeQuery) AllX(ctx context.Context) []*GoodIdentificationType {
	nodes, err := gitq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GoodIdentificationType IDs.
func (gitq *GoodIdentificationTypeQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := gitq.Select(goodidentificationtype.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (gitq *GoodIdentificationTypeQuery) IDsX(ctx context.Context) []int {
	ids, err := gitq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (gitq *GoodIdentificationTypeQuery) Count(ctx context.Context) (int, error) {
	if err := gitq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return gitq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (gitq *GoodIdentificationTypeQuery) CountX(ctx context.Context) int {
	count, err := gitq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (gitq *GoodIdentificationTypeQuery) Exist(ctx context.Context) (bool, error) {
	if err := gitq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return gitq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (gitq *GoodIdentificationTypeQuery) ExistX(ctx context.Context) bool {
	exist, err := gitq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GoodIdentificationTypeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (gitq *GoodIdentificationTypeQuery) Clone() *GoodIdentificationTypeQuery {
	if gitq == nil {
		return nil
	}
	return &GoodIdentificationTypeQuery{
		config:                           gitq.config,
		limit:                            gitq.limit,
		offset:                           gitq.offset,
		order:                            append([]OrderFunc{}, gitq.order...),
		predicates:                       append([]predicate.GoodIdentificationType{}, gitq.predicates...),
		withParent:                       gitq.withParent.Clone(),
		withChildren:                     gitq.withChildren.Clone(),
		withChildGoodIdentificationTypes: gitq.withChildGoodIdentificationTypes.Clone(),
		// clone intermediate query.
		sql:  gitq.sql.Clone(),
		path: gitq.path,
	}
}

// WithParent tells the query-builder to eager-load the nodes that are connected to
// the "parent" edge. The optional arguments are used to configure the query builder of the edge.
func (gitq *GoodIdentificationTypeQuery) WithParent(opts ...func(*GoodIdentificationTypeQuery)) *GoodIdentificationTypeQuery {
	query := &GoodIdentificationTypeQuery{config: gitq.config}
	for _, opt := range opts {
		opt(query)
	}
	gitq.withParent = query
	return gitq
}

// WithChildren tells the query-builder to eager-load the nodes that are connected to
// the "children" edge. The optional arguments are used to configure the query builder of the edge.
func (gitq *GoodIdentificationTypeQuery) WithChildren(opts ...func(*GoodIdentificationTypeQuery)) *GoodIdentificationTypeQuery {
	query := &GoodIdentificationTypeQuery{config: gitq.config}
	for _, opt := range opts {
		opt(query)
	}
	gitq.withChildren = query
	return gitq
}

// WithChildGoodIdentificationTypes tells the query-builder to eager-load the nodes that are connected to
// the "child_good_identification_types" edge. The optional arguments are used to configure the query builder of the edge.
func (gitq *GoodIdentificationTypeQuery) WithChildGoodIdentificationTypes(opts ...func(*GoodIdentificationTypeQuery)) *GoodIdentificationTypeQuery {
	query := &GoodIdentificationTypeQuery{config: gitq.config}
	for _, opt := range opts {
		opt(query)
	}
	gitq.withChildGoodIdentificationTypes = query
	return gitq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GoodIdentificationType.Query().
//		GroupBy(goodidentificationtype.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (gitq *GoodIdentificationTypeQuery) GroupBy(field string, fields ...string) *GoodIdentificationTypeGroupBy {
	group := &GoodIdentificationTypeGroupBy{config: gitq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := gitq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return gitq.sqlQuery(ctx), nil
	}
	return group
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.GoodIdentificationType.Query().
//		Select(goodidentificationtype.FieldCreateTime).
//		Scan(ctx, &v)
//
func (gitq *GoodIdentificationTypeQuery) Select(field string, fields ...string) *GoodIdentificationTypeSelect {
	gitq.fields = append([]string{field}, fields...)
	return &GoodIdentificationTypeSelect{GoodIdentificationTypeQuery: gitq}
}

func (gitq *GoodIdentificationTypeQuery) prepareQuery(ctx context.Context) error {
	for _, f := range gitq.fields {
		if !goodidentificationtype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if gitq.path != nil {
		prev, err := gitq.path(ctx)
		if err != nil {
			return err
		}
		gitq.sql = prev
	}
	return nil
}

func (gitq *GoodIdentificationTypeQuery) sqlAll(ctx context.Context) ([]*GoodIdentificationType, error) {
	var (
		nodes       = []*GoodIdentificationType{}
		withFKs     = gitq.withFKs
		_spec       = gitq.querySpec()
		loadedTypes = [3]bool{
			gitq.withParent != nil,
			gitq.withChildren != nil,
			gitq.withChildGoodIdentificationTypes != nil,
		}
	)
	if gitq.withParent != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, goodidentificationtype.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		node := &GoodIdentificationType{config: gitq.config}
		nodes = append(nodes, node)
		return node.scanValues(columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("ent: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if err := sqlgraph.QueryNodes(ctx, gitq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := gitq.withParent; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*GoodIdentificationType)
		for i := range nodes {
			if nodes[i].good_identification_type_children == nil {
				continue
			}
			fk := *nodes[i].good_identification_type_children
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(goodidentificationtype.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "good_identification_type_children" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Parent = n
			}
		}
	}

	if query := gitq.withChildren; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int]*GoodIdentificationType)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.Children = []*GoodIdentificationType{}
		}
		query.withFKs = true
		query.Where(predicate.GoodIdentificationType(func(s *sql.Selector) {
			s.Where(sql.InValues(goodidentificationtype.ChildrenColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.good_identification_type_children
			if fk == nil {
				return nil, fmt.Errorf(`foreign-key "good_identification_type_children" is nil for node %v`, n.ID)
			}
			node, ok := nodeids[*fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "good_identification_type_children" returned %v for node %v`, *fk, n.ID)
			}
			node.Edges.Children = append(node.Edges.Children, n)
		}
	}

	if query := gitq.withChildGoodIdentificationTypes; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		ids := make(map[int]*GoodIdentificationType, len(nodes))
		for _, node := range nodes {
			ids[node.ID] = node
			fks = append(fks, node.ID)
			node.Edges.ChildGoodIdentificationTypes = []*GoodIdentificationType{}
		}
		var (
			edgeids []int
			edges   = make(map[int][]*GoodIdentificationType)
		)
		_spec := &sqlgraph.EdgeQuerySpec{
			Edge: &sqlgraph.EdgeSpec{
				Inverse: false,
				Table:   goodidentificationtype.ChildGoodIdentificationTypesTable,
				Columns: goodidentificationtype.ChildGoodIdentificationTypesPrimaryKey,
			},
			Predicate: func(s *sql.Selector) {
				s.Where(sql.InValues(goodidentificationtype.ChildGoodIdentificationTypesPrimaryKey[0], fks...))
			},
			ScanValues: func() [2]interface{} {
				return [2]interface{}{new(sql.NullInt64), new(sql.NullInt64)}
			},
			Assign: func(out, in interface{}) error {
				eout, ok := out.(*sql.NullInt64)
				if !ok || eout == nil {
					return fmt.Errorf("unexpected id value for edge-out")
				}
				ein, ok := in.(*sql.NullInt64)
				if !ok || ein == nil {
					return fmt.Errorf("unexpected id value for edge-in")
				}
				outValue := int(eout.Int64)
				inValue := int(ein.Int64)
				node, ok := ids[outValue]
				if !ok {
					return fmt.Errorf("unexpected node id in edges: %v", outValue)
				}
				if _, ok := edges[inValue]; !ok {
					edgeids = append(edgeids, inValue)
				}
				edges[inValue] = append(edges[inValue], node)
				return nil
			},
		}
		if err := sqlgraph.QueryEdges(ctx, gitq.driver, _spec); err != nil {
			return nil, fmt.Errorf(`query edges "child_good_identification_types": %w`, err)
		}
		query.Where(goodidentificationtype.IDIn(edgeids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := edges[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected "child_good_identification_types" node returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.ChildGoodIdentificationTypes = append(nodes[i].Edges.ChildGoodIdentificationTypes, n)
			}
		}
	}

	return nodes, nil
}

func (gitq *GoodIdentificationTypeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := gitq.querySpec()
	return sqlgraph.CountNodes(ctx, gitq.driver, _spec)
}

func (gitq *GoodIdentificationTypeQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := gitq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (gitq *GoodIdentificationTypeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   goodidentificationtype.Table,
			Columns: goodidentificationtype.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: goodidentificationtype.FieldID,
			},
		},
		From:   gitq.sql,
		Unique: true,
	}
	if unique := gitq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := gitq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, goodidentificationtype.FieldID)
		for i := range fields {
			if fields[i] != goodidentificationtype.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := gitq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := gitq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := gitq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := gitq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (gitq *GoodIdentificationTypeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(gitq.driver.Dialect())
	t1 := builder.Table(goodidentificationtype.Table)
	columns := gitq.fields
	if len(columns) == 0 {
		columns = goodidentificationtype.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if gitq.sql != nil {
		selector = gitq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	for _, p := range gitq.predicates {
		p(selector)
	}
	for _, p := range gitq.order {
		p(selector)
	}
	if offset := gitq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := gitq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// GoodIdentificationTypeGroupBy is the group-by builder for GoodIdentificationType entities.
type GoodIdentificationTypeGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gitgb *GoodIdentificationTypeGroupBy) Aggregate(fns ...AggregateFunc) *GoodIdentificationTypeGroupBy {
	gitgb.fns = append(gitgb.fns, fns...)
	return gitgb
}

// Scan applies the group-by query and scans the result into the given value.
func (gitgb *GoodIdentificationTypeGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := gitgb.path(ctx)
	if err != nil {
		return err
	}
	gitgb.sql = query
	return gitgb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (gitgb *GoodIdentificationTypeGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := gitgb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by.
// It is only allowed when executing a group-by query with one field.
func (gitgb *GoodIdentificationTypeGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(gitgb.fields) > 1 {
		return nil, errors.New("ent: GoodIdentificationTypeGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := gitgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (gitgb *GoodIdentificationTypeGroupBy) StringsX(ctx context.Context) []string {
	v, err := gitgb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (gitgb *GoodIdentificationTypeGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = gitgb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{goodidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: GoodIdentificationTypeGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (gitgb *GoodIdentificationTypeGroupBy) StringX(ctx context.Context) string {
	v, err := gitgb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by.
// It is only allowed when executing a group-by query with one field.
func (gitgb *GoodIdentificationTypeGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(gitgb.fields) > 1 {
		return nil, errors.New("ent: GoodIdentificationTypeGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := gitgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (gitgb *GoodIdentificationTypeGroupBy) IntsX(ctx context.Context) []int {
	v, err := gitgb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (gitgb *GoodIdentificationTypeGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = gitgb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{goodidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: GoodIdentificationTypeGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (gitgb *GoodIdentificationTypeGroupBy) IntX(ctx context.Context) int {
	v, err := gitgb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (gitgb *GoodIdentificationTypeGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(gitgb.fields) > 1 {
		return nil, errors.New("ent: GoodIdentificationTypeGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := gitgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (gitgb *GoodIdentificationTypeGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := gitgb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (gitgb *GoodIdentificationTypeGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = gitgb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{goodidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: GoodIdentificationTypeGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (gitgb *GoodIdentificationTypeGroupBy) Float64X(ctx context.Context) float64 {
	v, err := gitgb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by.
// It is only allowed when executing a group-by query with one field.
func (gitgb *GoodIdentificationTypeGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(gitgb.fields) > 1 {
		return nil, errors.New("ent: GoodIdentificationTypeGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := gitgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (gitgb *GoodIdentificationTypeGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := gitgb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (gitgb *GoodIdentificationTypeGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = gitgb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{goodidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: GoodIdentificationTypeGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (gitgb *GoodIdentificationTypeGroupBy) BoolX(ctx context.Context) bool {
	v, err := gitgb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (gitgb *GoodIdentificationTypeGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range gitgb.fields {
		if !goodidentificationtype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := gitgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gitgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (gitgb *GoodIdentificationTypeGroupBy) sqlQuery() *sql.Selector {
	selector := gitgb.sql.Select()
	aggregation := make([]string, 0, len(gitgb.fns))
	for _, fn := range gitgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(gitgb.fields)+len(gitgb.fns))
		for _, f := range gitgb.fields {
			columns = append(columns, selector.C(f))
		}
		for _, c := range aggregation {
			columns = append(columns, c)
		}
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(gitgb.fields...)...)
}

// GoodIdentificationTypeSelect is the builder for selecting fields of GoodIdentificationType entities.
type GoodIdentificationTypeSelect struct {
	*GoodIdentificationTypeQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (gits *GoodIdentificationTypeSelect) Scan(ctx context.Context, v interface{}) error {
	if err := gits.prepareQuery(ctx); err != nil {
		return err
	}
	gits.sql = gits.GoodIdentificationTypeQuery.sqlQuery(ctx)
	return gits.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (gits *GoodIdentificationTypeSelect) ScanX(ctx context.Context, v interface{}) {
	if err := gits.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from a selector. It is only allowed when selecting one field.
func (gits *GoodIdentificationTypeSelect) Strings(ctx context.Context) ([]string, error) {
	if len(gits.fields) > 1 {
		return nil, errors.New("ent: GoodIdentificationTypeSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := gits.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (gits *GoodIdentificationTypeSelect) StringsX(ctx context.Context) []string {
	v, err := gits.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a selector. It is only allowed when selecting one field.
func (gits *GoodIdentificationTypeSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = gits.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{goodidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: GoodIdentificationTypeSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (gits *GoodIdentificationTypeSelect) StringX(ctx context.Context) string {
	v, err := gits.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from a selector. It is only allowed when selecting one field.
func (gits *GoodIdentificationTypeSelect) Ints(ctx context.Context) ([]int, error) {
	if len(gits.fields) > 1 {
		return nil, errors.New("ent: GoodIdentificationTypeSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := gits.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (gits *GoodIdentificationTypeSelect) IntsX(ctx context.Context) []int {
	v, err := gits.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a selector. It is only allowed when selecting one field.
func (gits *GoodIdentificationTypeSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = gits.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{goodidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: GoodIdentificationTypeSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (gits *GoodIdentificationTypeSelect) IntX(ctx context.Context) int {
	v, err := gits.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from a selector. It is only allowed when selecting one field.
func (gits *GoodIdentificationTypeSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(gits.fields) > 1 {
		return nil, errors.New("ent: GoodIdentificationTypeSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := gits.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (gits *GoodIdentificationTypeSelect) Float64sX(ctx context.Context) []float64 {
	v, err := gits.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a selector. It is only allowed when selecting one field.
func (gits *GoodIdentificationTypeSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = gits.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{goodidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: GoodIdentificationTypeSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (gits *GoodIdentificationTypeSelect) Float64X(ctx context.Context) float64 {
	v, err := gits.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from a selector. It is only allowed when selecting one field.
func (gits *GoodIdentificationTypeSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(gits.fields) > 1 {
		return nil, errors.New("ent: GoodIdentificationTypeSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := gits.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (gits *GoodIdentificationTypeSelect) BoolsX(ctx context.Context) []bool {
	v, err := gits.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a selector. It is only allowed when selecting one field.
func (gits *GoodIdentificationTypeSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = gits.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{goodidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: GoodIdentificationTypeSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (gits *GoodIdentificationTypeSelect) BoolX(ctx context.Context) bool {
	v, err := gits.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (gits *GoodIdentificationTypeSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := gits.sql.Query()
	if err := gits.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
