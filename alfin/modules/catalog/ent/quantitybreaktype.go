// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/samlet/petrel/alfin/modules/catalog/ent/quantitybreaktype"
)

// QuantityBreakType is the model entity for the QuantityBreakType schema.
type QuantityBreakType struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// StringRef holds the value of the "string_ref" field.
	StringRef string `json:"string_ref,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*QuantityBreakType) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case quantitybreaktype.FieldID:
			values[i] = new(sql.NullInt64)
		case quantitybreaktype.FieldStringRef, quantitybreaktype.FieldDescription:
			values[i] = new(sql.NullString)
		case quantitybreaktype.FieldCreateTime, quantitybreaktype.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type QuantityBreakType", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the QuantityBreakType fields.
func (qbt *QuantityBreakType) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case quantitybreaktype.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			qbt.ID = int(value.Int64)
		case quantitybreaktype.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				qbt.CreateTime = value.Time
			}
		case quantitybreaktype.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				qbt.UpdateTime = value.Time
			}
		case quantitybreaktype.FieldStringRef:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field string_ref", values[i])
			} else if value.Valid {
				qbt.StringRef = value.String
			}
		case quantitybreaktype.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				qbt.Description = value.String
			}
		}
	}
	return nil
}

// Update returns a builder for updating this QuantityBreakType.
// Note that you need to call QuantityBreakType.Unwrap() before calling this method if this QuantityBreakType
// was returned from a transaction, and the transaction was committed or rolled back.
func (qbt *QuantityBreakType) Update() *QuantityBreakTypeUpdateOne {
	return (&QuantityBreakTypeClient{config: qbt.config}).UpdateOne(qbt)
}

// Unwrap unwraps the QuantityBreakType entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (qbt *QuantityBreakType) Unwrap() *QuantityBreakType {
	tx, ok := qbt.config.driver.(*txDriver)
	if !ok {
		panic("ent: QuantityBreakType is not a transactional entity")
	}
	qbt.config.driver = tx.drv
	return qbt
}

// String implements the fmt.Stringer.
func (qbt *QuantityBreakType) String() string {
	var builder strings.Builder
	builder.WriteString("QuantityBreakType(")
	builder.WriteString(fmt.Sprintf("id=%v", qbt.ID))
	builder.WriteString(", create_time=")
	builder.WriteString(qbt.CreateTime.Format(time.ANSIC))
	builder.WriteString(", update_time=")
	builder.WriteString(qbt.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", string_ref=")
	builder.WriteString(qbt.StringRef)
	builder.WriteString(", description=")
	builder.WriteString(qbt.Description)
	builder.WriteByte(')')
	return builder.String()
}

// QuantityBreakTypes is a parsable slice of QuantityBreakType.
type QuantityBreakTypes []*QuantityBreakType

func (qbt QuantityBreakTypes) config(cfg config) {
	for _i := range qbt {
		qbt[_i].config = cfg
	}
}
