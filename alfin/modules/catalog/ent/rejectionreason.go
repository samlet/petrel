// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/samlet/petrel/alfin/modules/catalog/ent/rejectionreason"
)

// RejectionReason is the model entity for the RejectionReason schema.
type RejectionReason struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// StringRef holds the value of the "string_ref" field.
	StringRef string `json:"string_ref,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RejectionReason) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case rejectionreason.FieldID:
			values[i] = new(sql.NullInt64)
		case rejectionreason.FieldStringRef, rejectionreason.FieldDescription:
			values[i] = new(sql.NullString)
		case rejectionreason.FieldCreateTime, rejectionreason.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type RejectionReason", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RejectionReason fields.
func (rr *RejectionReason) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case rejectionreason.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			rr.ID = int(value.Int64)
		case rejectionreason.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				rr.CreateTime = value.Time
			}
		case rejectionreason.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				rr.UpdateTime = value.Time
			}
		case rejectionreason.FieldStringRef:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field string_ref", values[i])
			} else if value.Valid {
				rr.StringRef = value.String
			}
		case rejectionreason.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				rr.Description = value.String
			}
		}
	}
	return nil
}

// Update returns a builder for updating this RejectionReason.
// Note that you need to call RejectionReason.Unwrap() before calling this method if this RejectionReason
// was returned from a transaction, and the transaction was committed or rolled back.
func (rr *RejectionReason) Update() *RejectionReasonUpdateOne {
	return (&RejectionReasonClient{config: rr.config}).UpdateOne(rr)
}

// Unwrap unwraps the RejectionReason entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rr *RejectionReason) Unwrap() *RejectionReason {
	tx, ok := rr.config.driver.(*txDriver)
	if !ok {
		panic("ent: RejectionReason is not a transactional entity")
	}
	rr.config.driver = tx.drv
	return rr
}

// String implements the fmt.Stringer.
func (rr *RejectionReason) String() string {
	var builder strings.Builder
	builder.WriteString("RejectionReason(")
	builder.WriteString(fmt.Sprintf("id=%v", rr.ID))
	builder.WriteString(", create_time=")
	builder.WriteString(rr.CreateTime.Format(time.ANSIC))
	builder.WriteString(", update_time=")
	builder.WriteString(rr.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", string_ref=")
	builder.WriteString(rr.StringRef)
	builder.WriteString(", description=")
	builder.WriteString(rr.Description)
	builder.WriteByte(')')
	return builder.String()
}

// RejectionReasons is a parsable slice of RejectionReason.
type RejectionReasons []*RejectionReason

func (rr RejectionReasons) config(cfg config) {
	for _i := range rr {
		rr[_i].config = cfg
	}
}
