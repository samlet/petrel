// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/samlet/petrel/alfin/modules/workeffort/ent/party"
	"github.com/samlet/petrel/alfin/modules/workeffort/ent/partycontactmech"
	"github.com/samlet/petrel/alfin/modules/workeffort/ent/partyrole"
	"github.com/samlet/petrel/alfin/modules/workeffort/ent/person"
	"github.com/samlet/petrel/alfin/modules/workeffort/ent/predicate"
	"github.com/samlet/petrel/alfin/modules/workeffort/ent/roletype"
)

// PartyContactMechQuery is the builder for querying PartyContactMech entities.
type PartyContactMechQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.PartyContactMech
	// eager-loading edges.
	withParty     *PartyQuery
	withPerson    *PersonQuery
	withPartyRole *PartyRoleQuery
	withRoleType  *RoleTypeQuery
	withFKs       bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PartyContactMechQuery builder.
func (pcmq *PartyContactMechQuery) Where(ps ...predicate.PartyContactMech) *PartyContactMechQuery {
	pcmq.predicates = append(pcmq.predicates, ps...)
	return pcmq
}

// Limit adds a limit step to the query.
func (pcmq *PartyContactMechQuery) Limit(limit int) *PartyContactMechQuery {
	pcmq.limit = &limit
	return pcmq
}

// Offset adds an offset step to the query.
func (pcmq *PartyContactMechQuery) Offset(offset int) *PartyContactMechQuery {
	pcmq.offset = &offset
	return pcmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pcmq *PartyContactMechQuery) Unique(unique bool) *PartyContactMechQuery {
	pcmq.unique = &unique
	return pcmq
}

// Order adds an order step to the query.
func (pcmq *PartyContactMechQuery) Order(o ...OrderFunc) *PartyContactMechQuery {
	pcmq.order = append(pcmq.order, o...)
	return pcmq
}

// QueryParty chains the current query on the "party" edge.
func (pcmq *PartyContactMechQuery) QueryParty() *PartyQuery {
	query := &PartyQuery{config: pcmq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pcmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pcmq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(partycontactmech.Table, partycontactmech.FieldID, selector),
			sqlgraph.To(party.Table, party.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, partycontactmech.PartyTable, partycontactmech.PartyColumn),
		)
		fromU = sqlgraph.SetNeighbors(pcmq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPerson chains the current query on the "person" edge.
func (pcmq *PartyContactMechQuery) QueryPerson() *PersonQuery {
	query := &PersonQuery{config: pcmq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pcmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pcmq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(partycontactmech.Table, partycontactmech.FieldID, selector),
			sqlgraph.To(person.Table, person.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, partycontactmech.PersonTable, partycontactmech.PersonColumn),
		)
		fromU = sqlgraph.SetNeighbors(pcmq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPartyRole chains the current query on the "party_role" edge.
func (pcmq *PartyContactMechQuery) QueryPartyRole() *PartyRoleQuery {
	query := &PartyRoleQuery{config: pcmq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pcmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pcmq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(partycontactmech.Table, partycontactmech.FieldID, selector),
			sqlgraph.To(partyrole.Table, partyrole.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, partycontactmech.PartyRoleTable, partycontactmech.PartyRoleColumn),
		)
		fromU = sqlgraph.SetNeighbors(pcmq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRoleType chains the current query on the "role_type" edge.
func (pcmq *PartyContactMechQuery) QueryRoleType() *RoleTypeQuery {
	query := &RoleTypeQuery{config: pcmq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pcmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pcmq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(partycontactmech.Table, partycontactmech.FieldID, selector),
			sqlgraph.To(roletype.Table, roletype.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, partycontactmech.RoleTypeTable, partycontactmech.RoleTypeColumn),
		)
		fromU = sqlgraph.SetNeighbors(pcmq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PartyContactMech entity from the query.
// Returns a *NotFoundError when no PartyContactMech was found.
func (pcmq *PartyContactMechQuery) First(ctx context.Context) (*PartyContactMech, error) {
	nodes, err := pcmq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{partycontactmech.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pcmq *PartyContactMechQuery) FirstX(ctx context.Context) *PartyContactMech {
	node, err := pcmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PartyContactMech ID from the query.
// Returns a *NotFoundError when no PartyContactMech ID was found.
func (pcmq *PartyContactMechQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pcmq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{partycontactmech.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pcmq *PartyContactMechQuery) FirstIDX(ctx context.Context) int {
	id, err := pcmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PartyContactMech entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when exactly one PartyContactMech entity is not found.
// Returns a *NotFoundError when no PartyContactMech entities are found.
func (pcmq *PartyContactMechQuery) Only(ctx context.Context) (*PartyContactMech, error) {
	nodes, err := pcmq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{partycontactmech.Label}
	default:
		return nil, &NotSingularError{partycontactmech.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pcmq *PartyContactMechQuery) OnlyX(ctx context.Context) *PartyContactMech {
	node, err := pcmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PartyContactMech ID in the query.
// Returns a *NotSingularError when exactly one PartyContactMech ID is not found.
// Returns a *NotFoundError when no entities are found.
func (pcmq *PartyContactMechQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pcmq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{partycontactmech.Label}
	default:
		err = &NotSingularError{partycontactmech.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pcmq *PartyContactMechQuery) OnlyIDX(ctx context.Context) int {
	id, err := pcmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PartyContactMeches.
func (pcmq *PartyContactMechQuery) All(ctx context.Context) ([]*PartyContactMech, error) {
	if err := pcmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return pcmq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (pcmq *PartyContactMechQuery) AllX(ctx context.Context) []*PartyContactMech {
	nodes, err := pcmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PartyContactMech IDs.
func (pcmq *PartyContactMechQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := pcmq.Select(partycontactmech.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pcmq *PartyContactMechQuery) IDsX(ctx context.Context) []int {
	ids, err := pcmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pcmq *PartyContactMechQuery) Count(ctx context.Context) (int, error) {
	if err := pcmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return pcmq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (pcmq *PartyContactMechQuery) CountX(ctx context.Context) int {
	count, err := pcmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pcmq *PartyContactMechQuery) Exist(ctx context.Context) (bool, error) {
	if err := pcmq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return pcmq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (pcmq *PartyContactMechQuery) ExistX(ctx context.Context) bool {
	exist, err := pcmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PartyContactMechQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pcmq *PartyContactMechQuery) Clone() *PartyContactMechQuery {
	if pcmq == nil {
		return nil
	}
	return &PartyContactMechQuery{
		config:        pcmq.config,
		limit:         pcmq.limit,
		offset:        pcmq.offset,
		order:         append([]OrderFunc{}, pcmq.order...),
		predicates:    append([]predicate.PartyContactMech{}, pcmq.predicates...),
		withParty:     pcmq.withParty.Clone(),
		withPerson:    pcmq.withPerson.Clone(),
		withPartyRole: pcmq.withPartyRole.Clone(),
		withRoleType:  pcmq.withRoleType.Clone(),
		// clone intermediate query.
		sql:  pcmq.sql.Clone(),
		path: pcmq.path,
	}
}

// WithParty tells the query-builder to eager-load the nodes that are connected to
// the "party" edge. The optional arguments are used to configure the query builder of the edge.
func (pcmq *PartyContactMechQuery) WithParty(opts ...func(*PartyQuery)) *PartyContactMechQuery {
	query := &PartyQuery{config: pcmq.config}
	for _, opt := range opts {
		opt(query)
	}
	pcmq.withParty = query
	return pcmq
}

// WithPerson tells the query-builder to eager-load the nodes that are connected to
// the "person" edge. The optional arguments are used to configure the query builder of the edge.
func (pcmq *PartyContactMechQuery) WithPerson(opts ...func(*PersonQuery)) *PartyContactMechQuery {
	query := &PersonQuery{config: pcmq.config}
	for _, opt := range opts {
		opt(query)
	}
	pcmq.withPerson = query
	return pcmq
}

// WithPartyRole tells the query-builder to eager-load the nodes that are connected to
// the "party_role" edge. The optional arguments are used to configure the query builder of the edge.
func (pcmq *PartyContactMechQuery) WithPartyRole(opts ...func(*PartyRoleQuery)) *PartyContactMechQuery {
	query := &PartyRoleQuery{config: pcmq.config}
	for _, opt := range opts {
		opt(query)
	}
	pcmq.withPartyRole = query
	return pcmq
}

// WithRoleType tells the query-builder to eager-load the nodes that are connected to
// the "role_type" edge. The optional arguments are used to configure the query builder of the edge.
func (pcmq *PartyContactMechQuery) WithRoleType(opts ...func(*RoleTypeQuery)) *PartyContactMechQuery {
	query := &RoleTypeQuery{config: pcmq.config}
	for _, opt := range opts {
		opt(query)
	}
	pcmq.withRoleType = query
	return pcmq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PartyContactMech.Query().
//		GroupBy(partycontactmech.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (pcmq *PartyContactMechQuery) GroupBy(field string, fields ...string) *PartyContactMechGroupBy {
	group := &PartyContactMechGroupBy{config: pcmq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := pcmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return pcmq.sqlQuery(ctx), nil
	}
	return group
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.PartyContactMech.Query().
//		Select(partycontactmech.FieldCreateTime).
//		Scan(ctx, &v)
//
func (pcmq *PartyContactMechQuery) Select(field string, fields ...string) *PartyContactMechSelect {
	pcmq.fields = append([]string{field}, fields...)
	return &PartyContactMechSelect{PartyContactMechQuery: pcmq}
}

func (pcmq *PartyContactMechQuery) prepareQuery(ctx context.Context) error {
	for _, f := range pcmq.fields {
		if !partycontactmech.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pcmq.path != nil {
		prev, err := pcmq.path(ctx)
		if err != nil {
			return err
		}
		pcmq.sql = prev
	}
	return nil
}

func (pcmq *PartyContactMechQuery) sqlAll(ctx context.Context) ([]*PartyContactMech, error) {
	var (
		nodes       = []*PartyContactMech{}
		withFKs     = pcmq.withFKs
		_spec       = pcmq.querySpec()
		loadedTypes = [4]bool{
			pcmq.withParty != nil,
			pcmq.withPerson != nil,
			pcmq.withPartyRole != nil,
			pcmq.withRoleType != nil,
		}
	)
	if pcmq.withParty != nil || pcmq.withPerson != nil || pcmq.withPartyRole != nil || pcmq.withRoleType != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, partycontactmech.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		node := &PartyContactMech{config: pcmq.config}
		nodes = append(nodes, node)
		return node.scanValues(columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("ent: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if err := sqlgraph.QueryNodes(ctx, pcmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := pcmq.withParty; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*PartyContactMech)
		for i := range nodes {
			if nodes[i].party_party_contact_meches == nil {
				continue
			}
			fk := *nodes[i].party_party_contact_meches
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(party.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "party_party_contact_meches" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Party = n
			}
		}
	}

	if query := pcmq.withPerson; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*PartyContactMech)
		for i := range nodes {
			if nodes[i].person_party_contact_meches == nil {
				continue
			}
			fk := *nodes[i].person_party_contact_meches
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(person.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "person_party_contact_meches" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Person = n
			}
		}
	}

	if query := pcmq.withPartyRole; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*PartyContactMech)
		for i := range nodes {
			if nodes[i].party_role_party_contact_meches == nil {
				continue
			}
			fk := *nodes[i].party_role_party_contact_meches
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(partyrole.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "party_role_party_contact_meches" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.PartyRole = n
			}
		}
	}

	if query := pcmq.withRoleType; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*PartyContactMech)
		for i := range nodes {
			if nodes[i].role_type_party_contact_meches == nil {
				continue
			}
			fk := *nodes[i].role_type_party_contact_meches
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(roletype.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "role_type_party_contact_meches" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.RoleType = n
			}
		}
	}

	return nodes, nil
}

func (pcmq *PartyContactMechQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pcmq.querySpec()
	return sqlgraph.CountNodes(ctx, pcmq.driver, _spec)
}

func (pcmq *PartyContactMechQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := pcmq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (pcmq *PartyContactMechQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   partycontactmech.Table,
			Columns: partycontactmech.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: partycontactmech.FieldID,
			},
		},
		From:   pcmq.sql,
		Unique: true,
	}
	if unique := pcmq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := pcmq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, partycontactmech.FieldID)
		for i := range fields {
			if fields[i] != partycontactmech.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pcmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pcmq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pcmq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pcmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pcmq *PartyContactMechQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pcmq.driver.Dialect())
	t1 := builder.Table(partycontactmech.Table)
	columns := pcmq.fields
	if len(columns) == 0 {
		columns = partycontactmech.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pcmq.sql != nil {
		selector = pcmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	for _, p := range pcmq.predicates {
		p(selector)
	}
	for _, p := range pcmq.order {
		p(selector)
	}
	if offset := pcmq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pcmq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PartyContactMechGroupBy is the group-by builder for PartyContactMech entities.
type PartyContactMechGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pcmgb *PartyContactMechGroupBy) Aggregate(fns ...AggregateFunc) *PartyContactMechGroupBy {
	pcmgb.fns = append(pcmgb.fns, fns...)
	return pcmgb
}

// Scan applies the group-by query and scans the result into the given value.
func (pcmgb *PartyContactMechGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := pcmgb.path(ctx)
	if err != nil {
		return err
	}
	pcmgb.sql = query
	return pcmgb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (pcmgb *PartyContactMechGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := pcmgb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by.
// It is only allowed when executing a group-by query with one field.
func (pcmgb *PartyContactMechGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(pcmgb.fields) > 1 {
		return nil, errors.New("ent: PartyContactMechGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := pcmgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (pcmgb *PartyContactMechGroupBy) StringsX(ctx context.Context) []string {
	v, err := pcmgb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pcmgb *PartyContactMechGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = pcmgb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partycontactmech.Label}
	default:
		err = fmt.Errorf("ent: PartyContactMechGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (pcmgb *PartyContactMechGroupBy) StringX(ctx context.Context) string {
	v, err := pcmgb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by.
// It is only allowed when executing a group-by query with one field.
func (pcmgb *PartyContactMechGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(pcmgb.fields) > 1 {
		return nil, errors.New("ent: PartyContactMechGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := pcmgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (pcmgb *PartyContactMechGroupBy) IntsX(ctx context.Context) []int {
	v, err := pcmgb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pcmgb *PartyContactMechGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = pcmgb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partycontactmech.Label}
	default:
		err = fmt.Errorf("ent: PartyContactMechGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (pcmgb *PartyContactMechGroupBy) IntX(ctx context.Context) int {
	v, err := pcmgb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (pcmgb *PartyContactMechGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(pcmgb.fields) > 1 {
		return nil, errors.New("ent: PartyContactMechGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := pcmgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (pcmgb *PartyContactMechGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := pcmgb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pcmgb *PartyContactMechGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = pcmgb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partycontactmech.Label}
	default:
		err = fmt.Errorf("ent: PartyContactMechGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (pcmgb *PartyContactMechGroupBy) Float64X(ctx context.Context) float64 {
	v, err := pcmgb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by.
// It is only allowed when executing a group-by query with one field.
func (pcmgb *PartyContactMechGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(pcmgb.fields) > 1 {
		return nil, errors.New("ent: PartyContactMechGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := pcmgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (pcmgb *PartyContactMechGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := pcmgb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pcmgb *PartyContactMechGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = pcmgb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partycontactmech.Label}
	default:
		err = fmt.Errorf("ent: PartyContactMechGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (pcmgb *PartyContactMechGroupBy) BoolX(ctx context.Context) bool {
	v, err := pcmgb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (pcmgb *PartyContactMechGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range pcmgb.fields {
		if !partycontactmech.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := pcmgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pcmgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (pcmgb *PartyContactMechGroupBy) sqlQuery() *sql.Selector {
	selector := pcmgb.sql.Select()
	aggregation := make([]string, 0, len(pcmgb.fns))
	for _, fn := range pcmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(pcmgb.fields)+len(pcmgb.fns))
		for _, f := range pcmgb.fields {
			columns = append(columns, selector.C(f))
		}
		for _, c := range aggregation {
			columns = append(columns, c)
		}
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(pcmgb.fields...)...)
}

// PartyContactMechSelect is the builder for selecting fields of PartyContactMech entities.
type PartyContactMechSelect struct {
	*PartyContactMechQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (pcms *PartyContactMechSelect) Scan(ctx context.Context, v interface{}) error {
	if err := pcms.prepareQuery(ctx); err != nil {
		return err
	}
	pcms.sql = pcms.PartyContactMechQuery.sqlQuery(ctx)
	return pcms.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (pcms *PartyContactMechSelect) ScanX(ctx context.Context, v interface{}) {
	if err := pcms.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from a selector. It is only allowed when selecting one field.
func (pcms *PartyContactMechSelect) Strings(ctx context.Context) ([]string, error) {
	if len(pcms.fields) > 1 {
		return nil, errors.New("ent: PartyContactMechSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := pcms.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (pcms *PartyContactMechSelect) StringsX(ctx context.Context) []string {
	v, err := pcms.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a selector. It is only allowed when selecting one field.
func (pcms *PartyContactMechSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = pcms.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partycontactmech.Label}
	default:
		err = fmt.Errorf("ent: PartyContactMechSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (pcms *PartyContactMechSelect) StringX(ctx context.Context) string {
	v, err := pcms.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from a selector. It is only allowed when selecting one field.
func (pcms *PartyContactMechSelect) Ints(ctx context.Context) ([]int, error) {
	if len(pcms.fields) > 1 {
		return nil, errors.New("ent: PartyContactMechSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := pcms.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (pcms *PartyContactMechSelect) IntsX(ctx context.Context) []int {
	v, err := pcms.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a selector. It is only allowed when selecting one field.
func (pcms *PartyContactMechSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = pcms.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partycontactmech.Label}
	default:
		err = fmt.Errorf("ent: PartyContactMechSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (pcms *PartyContactMechSelect) IntX(ctx context.Context) int {
	v, err := pcms.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from a selector. It is only allowed when selecting one field.
func (pcms *PartyContactMechSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(pcms.fields) > 1 {
		return nil, errors.New("ent: PartyContactMechSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := pcms.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (pcms *PartyContactMechSelect) Float64sX(ctx context.Context) []float64 {
	v, err := pcms.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a selector. It is only allowed when selecting one field.
func (pcms *PartyContactMechSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = pcms.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partycontactmech.Label}
	default:
		err = fmt.Errorf("ent: PartyContactMechSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (pcms *PartyContactMechSelect) Float64X(ctx context.Context) float64 {
	v, err := pcms.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from a selector. It is only allowed when selecting one field.
func (pcms *PartyContactMechSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(pcms.fields) > 1 {
		return nil, errors.New("ent: PartyContactMechSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := pcms.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (pcms *PartyContactMechSelect) BoolsX(ctx context.Context) []bool {
	v, err := pcms.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a selector. It is only allowed when selecting one field.
func (pcms *PartyContactMechSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = pcms.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partycontactmech.Label}
	default:
		err = fmt.Errorf("ent: PartyContactMechSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (pcms *PartyContactMechSelect) BoolX(ctx context.Context) bool {
	v, err := pcms.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (pcms *PartyContactMechSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := pcms.sql.Query()
	if err := pcms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
