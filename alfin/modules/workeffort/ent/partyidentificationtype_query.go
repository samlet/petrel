// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/samlet/petrel/alfin/modules/workeffort/ent/partyidentificationtype"
	"github.com/samlet/petrel/alfin/modules/workeffort/ent/predicate"
)

// PartyIdentificationTypeQuery is the builder for querying PartyIdentificationType entities.
type PartyIdentificationTypeQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.PartyIdentificationType
	// eager-loading edges.
	withParent                        *PartyIdentificationTypeQuery
	withChildren                      *PartyIdentificationTypeQuery
	withChildPartyIdentificationTypes *PartyIdentificationTypeQuery
	withFKs                           bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PartyIdentificationTypeQuery builder.
func (pitq *PartyIdentificationTypeQuery) Where(ps ...predicate.PartyIdentificationType) *PartyIdentificationTypeQuery {
	pitq.predicates = append(pitq.predicates, ps...)
	return pitq
}

// Limit adds a limit step to the query.
func (pitq *PartyIdentificationTypeQuery) Limit(limit int) *PartyIdentificationTypeQuery {
	pitq.limit = &limit
	return pitq
}

// Offset adds an offset step to the query.
func (pitq *PartyIdentificationTypeQuery) Offset(offset int) *PartyIdentificationTypeQuery {
	pitq.offset = &offset
	return pitq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pitq *PartyIdentificationTypeQuery) Unique(unique bool) *PartyIdentificationTypeQuery {
	pitq.unique = &unique
	return pitq
}

// Order adds an order step to the query.
func (pitq *PartyIdentificationTypeQuery) Order(o ...OrderFunc) *PartyIdentificationTypeQuery {
	pitq.order = append(pitq.order, o...)
	return pitq
}

// QueryParent chains the current query on the "parent" edge.
func (pitq *PartyIdentificationTypeQuery) QueryParent() *PartyIdentificationTypeQuery {
	query := &PartyIdentificationTypeQuery{config: pitq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pitq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pitq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(partyidentificationtype.Table, partyidentificationtype.FieldID, selector),
			sqlgraph.To(partyidentificationtype.Table, partyidentificationtype.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, partyidentificationtype.ParentTable, partyidentificationtype.ParentColumn),
		)
		fromU = sqlgraph.SetNeighbors(pitq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChildren chains the current query on the "children" edge.
func (pitq *PartyIdentificationTypeQuery) QueryChildren() *PartyIdentificationTypeQuery {
	query := &PartyIdentificationTypeQuery{config: pitq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pitq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pitq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(partyidentificationtype.Table, partyidentificationtype.FieldID, selector),
			sqlgraph.To(partyidentificationtype.Table, partyidentificationtype.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, partyidentificationtype.ChildrenTable, partyidentificationtype.ChildrenColumn),
		)
		fromU = sqlgraph.SetNeighbors(pitq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChildPartyIdentificationTypes chains the current query on the "child_party_identification_types" edge.
func (pitq *PartyIdentificationTypeQuery) QueryChildPartyIdentificationTypes() *PartyIdentificationTypeQuery {
	query := &PartyIdentificationTypeQuery{config: pitq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pitq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pitq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(partyidentificationtype.Table, partyidentificationtype.FieldID, selector),
			sqlgraph.To(partyidentificationtype.Table, partyidentificationtype.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, partyidentificationtype.ChildPartyIdentificationTypesTable, partyidentificationtype.ChildPartyIdentificationTypesPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(pitq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PartyIdentificationType entity from the query.
// Returns a *NotFoundError when no PartyIdentificationType was found.
func (pitq *PartyIdentificationTypeQuery) First(ctx context.Context) (*PartyIdentificationType, error) {
	nodes, err := pitq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{partyidentificationtype.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pitq *PartyIdentificationTypeQuery) FirstX(ctx context.Context) *PartyIdentificationType {
	node, err := pitq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PartyIdentificationType ID from the query.
// Returns a *NotFoundError when no PartyIdentificationType ID was found.
func (pitq *PartyIdentificationTypeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pitq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{partyidentificationtype.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pitq *PartyIdentificationTypeQuery) FirstIDX(ctx context.Context) int {
	id, err := pitq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PartyIdentificationType entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when exactly one PartyIdentificationType entity is not found.
// Returns a *NotFoundError when no PartyIdentificationType entities are found.
func (pitq *PartyIdentificationTypeQuery) Only(ctx context.Context) (*PartyIdentificationType, error) {
	nodes, err := pitq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{partyidentificationtype.Label}
	default:
		return nil, &NotSingularError{partyidentificationtype.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pitq *PartyIdentificationTypeQuery) OnlyX(ctx context.Context) *PartyIdentificationType {
	node, err := pitq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PartyIdentificationType ID in the query.
// Returns a *NotSingularError when exactly one PartyIdentificationType ID is not found.
// Returns a *NotFoundError when no entities are found.
func (pitq *PartyIdentificationTypeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pitq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{partyidentificationtype.Label}
	default:
		err = &NotSingularError{partyidentificationtype.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pitq *PartyIdentificationTypeQuery) OnlyIDX(ctx context.Context) int {
	id, err := pitq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PartyIdentificationTypes.
func (pitq *PartyIdentificationTypeQuery) All(ctx context.Context) ([]*PartyIdentificationType, error) {
	if err := pitq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return pitq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (pitq *PartyIdentificationTypeQuery) AllX(ctx context.Context) []*PartyIdentificationType {
	nodes, err := pitq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PartyIdentificationType IDs.
func (pitq *PartyIdentificationTypeQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := pitq.Select(partyidentificationtype.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pitq *PartyIdentificationTypeQuery) IDsX(ctx context.Context) []int {
	ids, err := pitq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pitq *PartyIdentificationTypeQuery) Count(ctx context.Context) (int, error) {
	if err := pitq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return pitq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (pitq *PartyIdentificationTypeQuery) CountX(ctx context.Context) int {
	count, err := pitq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pitq *PartyIdentificationTypeQuery) Exist(ctx context.Context) (bool, error) {
	if err := pitq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return pitq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (pitq *PartyIdentificationTypeQuery) ExistX(ctx context.Context) bool {
	exist, err := pitq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PartyIdentificationTypeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pitq *PartyIdentificationTypeQuery) Clone() *PartyIdentificationTypeQuery {
	if pitq == nil {
		return nil
	}
	return &PartyIdentificationTypeQuery{
		config:                            pitq.config,
		limit:                             pitq.limit,
		offset:                            pitq.offset,
		order:                             append([]OrderFunc{}, pitq.order...),
		predicates:                        append([]predicate.PartyIdentificationType{}, pitq.predicates...),
		withParent:                        pitq.withParent.Clone(),
		withChildren:                      pitq.withChildren.Clone(),
		withChildPartyIdentificationTypes: pitq.withChildPartyIdentificationTypes.Clone(),
		// clone intermediate query.
		sql:  pitq.sql.Clone(),
		path: pitq.path,
	}
}

// WithParent tells the query-builder to eager-load the nodes that are connected to
// the "parent" edge. The optional arguments are used to configure the query builder of the edge.
func (pitq *PartyIdentificationTypeQuery) WithParent(opts ...func(*PartyIdentificationTypeQuery)) *PartyIdentificationTypeQuery {
	query := &PartyIdentificationTypeQuery{config: pitq.config}
	for _, opt := range opts {
		opt(query)
	}
	pitq.withParent = query
	return pitq
}

// WithChildren tells the query-builder to eager-load the nodes that are connected to
// the "children" edge. The optional arguments are used to configure the query builder of the edge.
func (pitq *PartyIdentificationTypeQuery) WithChildren(opts ...func(*PartyIdentificationTypeQuery)) *PartyIdentificationTypeQuery {
	query := &PartyIdentificationTypeQuery{config: pitq.config}
	for _, opt := range opts {
		opt(query)
	}
	pitq.withChildren = query
	return pitq
}

// WithChildPartyIdentificationTypes tells the query-builder to eager-load the nodes that are connected to
// the "child_party_identification_types" edge. The optional arguments are used to configure the query builder of the edge.
func (pitq *PartyIdentificationTypeQuery) WithChildPartyIdentificationTypes(opts ...func(*PartyIdentificationTypeQuery)) *PartyIdentificationTypeQuery {
	query := &PartyIdentificationTypeQuery{config: pitq.config}
	for _, opt := range opts {
		opt(query)
	}
	pitq.withChildPartyIdentificationTypes = query
	return pitq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PartyIdentificationType.Query().
//		GroupBy(partyidentificationtype.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (pitq *PartyIdentificationTypeQuery) GroupBy(field string, fields ...string) *PartyIdentificationTypeGroupBy {
	group := &PartyIdentificationTypeGroupBy{config: pitq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := pitq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return pitq.sqlQuery(ctx), nil
	}
	return group
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.PartyIdentificationType.Query().
//		Select(partyidentificationtype.FieldCreateTime).
//		Scan(ctx, &v)
//
func (pitq *PartyIdentificationTypeQuery) Select(field string, fields ...string) *PartyIdentificationTypeSelect {
	pitq.fields = append([]string{field}, fields...)
	return &PartyIdentificationTypeSelect{PartyIdentificationTypeQuery: pitq}
}

func (pitq *PartyIdentificationTypeQuery) prepareQuery(ctx context.Context) error {
	for _, f := range pitq.fields {
		if !partyidentificationtype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pitq.path != nil {
		prev, err := pitq.path(ctx)
		if err != nil {
			return err
		}
		pitq.sql = prev
	}
	return nil
}

func (pitq *PartyIdentificationTypeQuery) sqlAll(ctx context.Context) ([]*PartyIdentificationType, error) {
	var (
		nodes       = []*PartyIdentificationType{}
		withFKs     = pitq.withFKs
		_spec       = pitq.querySpec()
		loadedTypes = [3]bool{
			pitq.withParent != nil,
			pitq.withChildren != nil,
			pitq.withChildPartyIdentificationTypes != nil,
		}
	)
	if pitq.withParent != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, partyidentificationtype.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		node := &PartyIdentificationType{config: pitq.config}
		nodes = append(nodes, node)
		return node.scanValues(columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("ent: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if err := sqlgraph.QueryNodes(ctx, pitq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := pitq.withParent; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*PartyIdentificationType)
		for i := range nodes {
			if nodes[i].party_identification_type_children == nil {
				continue
			}
			fk := *nodes[i].party_identification_type_children
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(partyidentificationtype.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "party_identification_type_children" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Parent = n
			}
		}
	}

	if query := pitq.withChildren; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int]*PartyIdentificationType)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.Children = []*PartyIdentificationType{}
		}
		query.withFKs = true
		query.Where(predicate.PartyIdentificationType(func(s *sql.Selector) {
			s.Where(sql.InValues(partyidentificationtype.ChildrenColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.party_identification_type_children
			if fk == nil {
				return nil, fmt.Errorf(`foreign-key "party_identification_type_children" is nil for node %v`, n.ID)
			}
			node, ok := nodeids[*fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "party_identification_type_children" returned %v for node %v`, *fk, n.ID)
			}
			node.Edges.Children = append(node.Edges.Children, n)
		}
	}

	if query := pitq.withChildPartyIdentificationTypes; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		ids := make(map[int]*PartyIdentificationType, len(nodes))
		for _, node := range nodes {
			ids[node.ID] = node
			fks = append(fks, node.ID)
			node.Edges.ChildPartyIdentificationTypes = []*PartyIdentificationType{}
		}
		var (
			edgeids []int
			edges   = make(map[int][]*PartyIdentificationType)
		)
		_spec := &sqlgraph.EdgeQuerySpec{
			Edge: &sqlgraph.EdgeSpec{
				Inverse: false,
				Table:   partyidentificationtype.ChildPartyIdentificationTypesTable,
				Columns: partyidentificationtype.ChildPartyIdentificationTypesPrimaryKey,
			},
			Predicate: func(s *sql.Selector) {
				s.Where(sql.InValues(partyidentificationtype.ChildPartyIdentificationTypesPrimaryKey[0], fks...))
			},
			ScanValues: func() [2]interface{} {
				return [2]interface{}{new(sql.NullInt64), new(sql.NullInt64)}
			},
			Assign: func(out, in interface{}) error {
				eout, ok := out.(*sql.NullInt64)
				if !ok || eout == nil {
					return fmt.Errorf("unexpected id value for edge-out")
				}
				ein, ok := in.(*sql.NullInt64)
				if !ok || ein == nil {
					return fmt.Errorf("unexpected id value for edge-in")
				}
				outValue := int(eout.Int64)
				inValue := int(ein.Int64)
				node, ok := ids[outValue]
				if !ok {
					return fmt.Errorf("unexpected node id in edges: %v", outValue)
				}
				if _, ok := edges[inValue]; !ok {
					edgeids = append(edgeids, inValue)
				}
				edges[inValue] = append(edges[inValue], node)
				return nil
			},
		}
		if err := sqlgraph.QueryEdges(ctx, pitq.driver, _spec); err != nil {
			return nil, fmt.Errorf(`query edges "child_party_identification_types": %w`, err)
		}
		query.Where(partyidentificationtype.IDIn(edgeids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := edges[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected "child_party_identification_types" node returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.ChildPartyIdentificationTypes = append(nodes[i].Edges.ChildPartyIdentificationTypes, n)
			}
		}
	}

	return nodes, nil
}

func (pitq *PartyIdentificationTypeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pitq.querySpec()
	return sqlgraph.CountNodes(ctx, pitq.driver, _spec)
}

func (pitq *PartyIdentificationTypeQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := pitq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (pitq *PartyIdentificationTypeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   partyidentificationtype.Table,
			Columns: partyidentificationtype.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: partyidentificationtype.FieldID,
			},
		},
		From:   pitq.sql,
		Unique: true,
	}
	if unique := pitq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := pitq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, partyidentificationtype.FieldID)
		for i := range fields {
			if fields[i] != partyidentificationtype.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pitq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pitq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pitq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pitq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pitq *PartyIdentificationTypeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pitq.driver.Dialect())
	t1 := builder.Table(partyidentificationtype.Table)
	columns := pitq.fields
	if len(columns) == 0 {
		columns = partyidentificationtype.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pitq.sql != nil {
		selector = pitq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	for _, p := range pitq.predicates {
		p(selector)
	}
	for _, p := range pitq.order {
		p(selector)
	}
	if offset := pitq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pitq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PartyIdentificationTypeGroupBy is the group-by builder for PartyIdentificationType entities.
type PartyIdentificationTypeGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pitgb *PartyIdentificationTypeGroupBy) Aggregate(fns ...AggregateFunc) *PartyIdentificationTypeGroupBy {
	pitgb.fns = append(pitgb.fns, fns...)
	return pitgb
}

// Scan applies the group-by query and scans the result into the given value.
func (pitgb *PartyIdentificationTypeGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := pitgb.path(ctx)
	if err != nil {
		return err
	}
	pitgb.sql = query
	return pitgb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (pitgb *PartyIdentificationTypeGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := pitgb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by.
// It is only allowed when executing a group-by query with one field.
func (pitgb *PartyIdentificationTypeGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(pitgb.fields) > 1 {
		return nil, errors.New("ent: PartyIdentificationTypeGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := pitgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (pitgb *PartyIdentificationTypeGroupBy) StringsX(ctx context.Context) []string {
	v, err := pitgb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pitgb *PartyIdentificationTypeGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = pitgb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partyidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: PartyIdentificationTypeGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (pitgb *PartyIdentificationTypeGroupBy) StringX(ctx context.Context) string {
	v, err := pitgb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by.
// It is only allowed when executing a group-by query with one field.
func (pitgb *PartyIdentificationTypeGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(pitgb.fields) > 1 {
		return nil, errors.New("ent: PartyIdentificationTypeGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := pitgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (pitgb *PartyIdentificationTypeGroupBy) IntsX(ctx context.Context) []int {
	v, err := pitgb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pitgb *PartyIdentificationTypeGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = pitgb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partyidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: PartyIdentificationTypeGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (pitgb *PartyIdentificationTypeGroupBy) IntX(ctx context.Context) int {
	v, err := pitgb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (pitgb *PartyIdentificationTypeGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(pitgb.fields) > 1 {
		return nil, errors.New("ent: PartyIdentificationTypeGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := pitgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (pitgb *PartyIdentificationTypeGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := pitgb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pitgb *PartyIdentificationTypeGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = pitgb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partyidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: PartyIdentificationTypeGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (pitgb *PartyIdentificationTypeGroupBy) Float64X(ctx context.Context) float64 {
	v, err := pitgb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by.
// It is only allowed when executing a group-by query with one field.
func (pitgb *PartyIdentificationTypeGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(pitgb.fields) > 1 {
		return nil, errors.New("ent: PartyIdentificationTypeGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := pitgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (pitgb *PartyIdentificationTypeGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := pitgb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (pitgb *PartyIdentificationTypeGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = pitgb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partyidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: PartyIdentificationTypeGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (pitgb *PartyIdentificationTypeGroupBy) BoolX(ctx context.Context) bool {
	v, err := pitgb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (pitgb *PartyIdentificationTypeGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range pitgb.fields {
		if !partyidentificationtype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := pitgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pitgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (pitgb *PartyIdentificationTypeGroupBy) sqlQuery() *sql.Selector {
	selector := pitgb.sql.Select()
	aggregation := make([]string, 0, len(pitgb.fns))
	for _, fn := range pitgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(pitgb.fields)+len(pitgb.fns))
		for _, f := range pitgb.fields {
			columns = append(columns, selector.C(f))
		}
		for _, c := range aggregation {
			columns = append(columns, c)
		}
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(pitgb.fields...)...)
}

// PartyIdentificationTypeSelect is the builder for selecting fields of PartyIdentificationType entities.
type PartyIdentificationTypeSelect struct {
	*PartyIdentificationTypeQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (pits *PartyIdentificationTypeSelect) Scan(ctx context.Context, v interface{}) error {
	if err := pits.prepareQuery(ctx); err != nil {
		return err
	}
	pits.sql = pits.PartyIdentificationTypeQuery.sqlQuery(ctx)
	return pits.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (pits *PartyIdentificationTypeSelect) ScanX(ctx context.Context, v interface{}) {
	if err := pits.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from a selector. It is only allowed when selecting one field.
func (pits *PartyIdentificationTypeSelect) Strings(ctx context.Context) ([]string, error) {
	if len(pits.fields) > 1 {
		return nil, errors.New("ent: PartyIdentificationTypeSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := pits.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (pits *PartyIdentificationTypeSelect) StringsX(ctx context.Context) []string {
	v, err := pits.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a selector. It is only allowed when selecting one field.
func (pits *PartyIdentificationTypeSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = pits.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partyidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: PartyIdentificationTypeSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (pits *PartyIdentificationTypeSelect) StringX(ctx context.Context) string {
	v, err := pits.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from a selector. It is only allowed when selecting one field.
func (pits *PartyIdentificationTypeSelect) Ints(ctx context.Context) ([]int, error) {
	if len(pits.fields) > 1 {
		return nil, errors.New("ent: PartyIdentificationTypeSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := pits.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (pits *PartyIdentificationTypeSelect) IntsX(ctx context.Context) []int {
	v, err := pits.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a selector. It is only allowed when selecting one field.
func (pits *PartyIdentificationTypeSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = pits.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partyidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: PartyIdentificationTypeSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (pits *PartyIdentificationTypeSelect) IntX(ctx context.Context) int {
	v, err := pits.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from a selector. It is only allowed when selecting one field.
func (pits *PartyIdentificationTypeSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(pits.fields) > 1 {
		return nil, errors.New("ent: PartyIdentificationTypeSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := pits.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (pits *PartyIdentificationTypeSelect) Float64sX(ctx context.Context) []float64 {
	v, err := pits.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a selector. It is only allowed when selecting one field.
func (pits *PartyIdentificationTypeSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = pits.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partyidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: PartyIdentificationTypeSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (pits *PartyIdentificationTypeSelect) Float64X(ctx context.Context) float64 {
	v, err := pits.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from a selector. It is only allowed when selecting one field.
func (pits *PartyIdentificationTypeSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(pits.fields) > 1 {
		return nil, errors.New("ent: PartyIdentificationTypeSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := pits.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (pits *PartyIdentificationTypeSelect) BoolsX(ctx context.Context) []bool {
	v, err := pits.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a selector. It is only allowed when selecting one field.
func (pits *PartyIdentificationTypeSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = pits.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{partyidentificationtype.Label}
	default:
		err = fmt.Errorf("ent: PartyIdentificationTypeSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (pits *PartyIdentificationTypeSelect) BoolX(ctx context.Context) bool {
	v, err := pits.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (pits *PartyIdentificationTypeSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := pits.sql.Query()
	if err := pits.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
