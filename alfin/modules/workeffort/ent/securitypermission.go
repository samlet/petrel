// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/samlet/petrel/alfin/modules/workeffort/ent/securitypermission"
)

// SecurityPermission is the model entity for the SecurityPermission schema.
type SecurityPermission struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// StringRef holds the value of the "string_ref" field.
	StringRef string `json:"string_ref,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SecurityPermissionQuery when eager-loading is set.
	Edges SecurityPermissionEdges `json:"edges"`
}

// SecurityPermissionEdges holds the relations/edges for other nodes in the graph.
type SecurityPermissionEdges struct {
	// SecurityGroupPermissions holds the value of the security_group_permissions edge.
	SecurityGroupPermissions []*SecurityGroupPermission `json:"security_group_permissions,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// SecurityGroupPermissionsOrErr returns the SecurityGroupPermissions value or an error if the edge
// was not loaded in eager-loading.
func (e SecurityPermissionEdges) SecurityGroupPermissionsOrErr() ([]*SecurityGroupPermission, error) {
	if e.loadedTypes[0] {
		return e.SecurityGroupPermissions, nil
	}
	return nil, &NotLoadedError{edge: "security_group_permissions"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SecurityPermission) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case securitypermission.FieldID:
			values[i] = new(sql.NullInt64)
		case securitypermission.FieldStringRef, securitypermission.FieldDescription:
			values[i] = new(sql.NullString)
		case securitypermission.FieldCreateTime, securitypermission.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type SecurityPermission", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SecurityPermission fields.
func (sp *SecurityPermission) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case securitypermission.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sp.ID = int(value.Int64)
		case securitypermission.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				sp.CreateTime = value.Time
			}
		case securitypermission.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				sp.UpdateTime = value.Time
			}
		case securitypermission.FieldStringRef:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field string_ref", values[i])
			} else if value.Valid {
				sp.StringRef = value.String
			}
		case securitypermission.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				sp.Description = value.String
			}
		}
	}
	return nil
}

// QuerySecurityGroupPermissions queries the "security_group_permissions" edge of the SecurityPermission entity.
func (sp *SecurityPermission) QuerySecurityGroupPermissions() *SecurityGroupPermissionQuery {
	return (&SecurityPermissionClient{config: sp.config}).QuerySecurityGroupPermissions(sp)
}

// Update returns a builder for updating this SecurityPermission.
// Note that you need to call SecurityPermission.Unwrap() before calling this method if this SecurityPermission
// was returned from a transaction, and the transaction was committed or rolled back.
func (sp *SecurityPermission) Update() *SecurityPermissionUpdateOne {
	return (&SecurityPermissionClient{config: sp.config}).UpdateOne(sp)
}

// Unwrap unwraps the SecurityPermission entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sp *SecurityPermission) Unwrap() *SecurityPermission {
	tx, ok := sp.config.driver.(*txDriver)
	if !ok {
		panic("ent: SecurityPermission is not a transactional entity")
	}
	sp.config.driver = tx.drv
	return sp
}

// String implements the fmt.Stringer.
func (sp *SecurityPermission) String() string {
	var builder strings.Builder
	builder.WriteString("SecurityPermission(")
	builder.WriteString(fmt.Sprintf("id=%v", sp.ID))
	builder.WriteString(", create_time=")
	builder.WriteString(sp.CreateTime.Format(time.ANSIC))
	builder.WriteString(", update_time=")
	builder.WriteString(sp.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", string_ref=")
	builder.WriteString(sp.StringRef)
	builder.WriteString(", description=")
	builder.WriteString(sp.Description)
	builder.WriteByte(')')
	return builder.String()
}

// SecurityPermissions is a parsable slice of SecurityPermission.
type SecurityPermissions []*SecurityPermission

func (sp SecurityPermissions) config(cfg config) {
	for _i := range sp {
		sp[_i].config = cfg
	}
}
